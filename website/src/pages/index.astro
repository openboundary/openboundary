---
import HomeLayout from '../layouts/HomeLayout.astro';
import Terminal from '../components/Terminal.astro';
import TerminalLine from '../components/TerminalLine.astro';
import ValueGrid from '../components/ValueGrid.astro';
import StandardsGrid from '../components/StandardsGrid.astro';
import CodeComparison from '../components/CodeComparison.astro';
import ComponentsGrid from '../components/ComponentsGrid.astro';
import StableBoundary from '../components/StableBoundary.astro';

const github = 'https://github.com/openboundary/openboundary';
---

<HomeLayout title="OpenBoundary — Open-source specification compiler for backend architecture">
  <section class="hero">
    <div class="hero-badges">
      <span class="badge badge-oss">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="10"/>
          <path d="M12 16v-4M12 8h.01"/>
        </svg>
        Open Source
      </span>
      <span class="badge badge-license">AGPL-3.0</span>
    </div>

    <h1>Compile architectural<br/>constraints into code</h1>

    <p class="hero-sub">
      Define authentication, authorization, middleware chains, and database schemas in YAML.
      The compiler generates type-safe TypeScript that enforces your architecture.
    </p>

    <div class="hero-actions">
      <a href="/docs/getting-started" class="btn btn-primary">
        Get Started
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M12 5l7 7-7 7" />
        </svg>
      </a>
      <a href={github} class="btn btn-secondary">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
        View on GitHub
      </a>
    </div>

    <Terminal title="bound compile spec.yaml">
      <TerminalLine type="success">Validated specification</TerminalLine>
      <TerminalLine type="success">Enforced middleware invariants</TerminalLine>
      <TerminalLine type="success">Generated TypeScript project</TerminalLine>
      <TerminalLine type="muted">  src/server.ts</TerminalLine>
      <TerminalLine type="muted">  src/middleware/authn.ts</TerminalLine>
      <TerminalLine type="muted">  src/middleware/authz.ts</TerminalLine>
      <TerminalLine type="muted">  src/handlers/users.ts</TerminalLine>
    </Terminal>
  </section>

  <section class="section problem">
    <div class="section-grid">
      <div class="section-header">
        <div class="section-label">The Problem</div>
        <h2>AI tools generate code fast.<br/>They don't enforce architecture.</h2>
      </div>
      <div class="section-content">
        <p>
          AI coding assistants will generate an unauthenticated endpoint, skip rate limiting,
          or flatten your authorization model. Not out of malice — they don't have architectural context.
        </p>
        <p>
          That context lives in your head, scattered across docs, lost in code comments.
          It drifts from implementation. Nobody enforces it.
        </p>
        <p class="highlight">
          OpenBoundary makes architectural constraints explicit, machine-readable, and enforced at compile time.
        </p>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="section-header centered">
      <div class="section-label">How It Works</div>
      <h2>YAML in. TypeScript out.</h2>
      <p class="section-desc">
        Define your boundaries in a specification file. The compiler validates constraints and generates production-ready code.
      </p>
    </div>

    <CodeComparison />
  </section>

  <section class="section">
    <div class="section-header centered">
      <div class="section-label">Why OpenBoundary</div>
      <h2>Separate what changes rarely from what changes constantly</h2>
    </div>

    <ValueGrid />
  </section>

  <section class="section">
    <div class="section-header centered">
      <div class="section-label">No Lock-in</div>
      <h2>Built on open standards</h2>
      <p class="section-desc">
        Your specs reference existing ecosystem tools in their native formats. OpenBoundary orchestrates — it doesn't replace.
      </p>
    </div>

    <StandardsGrid />
  </section>

  <section class="section" id="components">
    <div class="section-header centered">
      <div class="section-label">Components</div>
      <h2>What you can define</h2>
      <p class="section-desc">
        HTTP servers, databases, middleware chains, and use cases. Each component type has its own validation rules.
      </p>
    </div>

    <ComponentsGrid />
  </section>

  <StableBoundary />

  <section class="cta">
    <div class="cta-inner">
      <div class="cta-badge">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
        </svg>
        Open Source
      </div>
      <h2>Start building with constraints</h2>
      <p>Clone the repo, try the example spec, read the docs.</p>
      <div class="cta-code">
        <code>curl -fsSL https://openboundary.org/install.sh | sh</code>
      </div>
      <div class="cta-actions">
        <a href="/docs/getting-started" class="btn btn-primary">
          Quick Start
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7" />
          </svg>
        </a>
        <a href={github} class="btn btn-secondary">GitHub</a>
      </div>
    </div>
  </section>
</HomeLayout>

<style>
  /* Hero Section */
  .hero {
    max-width: 900px;
    margin: 0 auto;
    padding: 140px 24px 100px;
    text-align: center;
  }

  .hero-badges {
    display: flex;
    justify-content: center;
    gap: 8px;
    margin-bottom: 32px;
  }

  .badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-family: var(--ob-font-display);
    font-size: 11px;
    font-weight: 500;
    padding: 6px 12px;
    border-radius: 2px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .badge-oss {
    color: var(--ob-accent);
    background: var(--ob-accent-dim);
    border: 1px solid rgba(94, 234, 212, 0.2);
  }

  .badge-license {
    color: var(--ob-text-tertiary);
    background: var(--ob-bg-elevated);
    border: 1px solid var(--ob-border);
  }

  .hero h1 {
    font-family: var(--ob-font-display);
    font-size: clamp(32px, 5vw, 52px);
    font-weight: 500;
    letter-spacing: -0.03em;
    line-height: 1.15;
    margin-bottom: 28px;
  }

  .hero-sub {
    font-size: 17px;
    color: var(--ob-text-secondary);
    max-width: 600px;
    margin: 0 auto 40px;
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    justify-content: center;
    gap: 12px;
    margin-bottom: 64px;
  }

  .hero-actions .btn {
    padding: 12px 20px;
    font-size: 14px;
  }

  /* Sections */
  .section {
    max-width: 1100px;
    margin: 0 auto;
    padding: 100px 24px;
    border-top: 1px solid var(--ob-border);
  }

  .section-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: start;
  }

  .section-header {
    margin-bottom: 48px;
  }

  .section-header.centered {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 56px;
  }

  .section-label {
    font-family: var(--ob-font-display);
    font-size: 11px;
    font-weight: 500;
    color: var(--ob-accent);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 16px;
  }

  .section h2 {
    font-family: var(--ob-font-display);
    font-size: clamp(24px, 3vw, 32px);
    font-weight: 500;
    letter-spacing: -0.02em;
    line-height: 1.25;
    margin-bottom: 16px;
  }

  .section-desc {
    font-size: 16px;
    color: var(--ob-text-secondary);
    line-height: 1.7;
  }

  .section-content p {
    font-size: 16px;
    color: var(--ob-text-secondary);
    line-height: 1.8;
    margin-bottom: 20px;
  }

  .section-content p:last-child {
    margin-bottom: 0;
  }

  .section-content p.highlight {
    color: var(--ob-text);
    font-weight: 500;
  }

  /* Problem Section */
  .problem {
    background: linear-gradient(180deg, var(--ob-bg) 0%, var(--ob-bg-subtle) 100%);
  }

  /* CTA Section */
  .cta {
    border-top: 1px solid var(--ob-border);
    padding: 100px 24px;
    background: var(--ob-bg-subtle);
  }

  .cta-inner {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
  }

  .cta-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-family: var(--ob-font-display);
    font-size: 11px;
    font-weight: 500;
    color: var(--ob-accent);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 20px;
  }

  .cta h2 {
    font-family: var(--ob-font-display);
    font-size: 28px;
    font-weight: 500;
    letter-spacing: -0.02em;
    margin-bottom: 12px;
  }

  .cta p {
    color: var(--ob-text-secondary);
    margin-bottom: 32px;
  }

  .cta-code {
    display: inline-block;
    font-family: var(--ob-font-mono);
    font-size: 13px;
    color: var(--ob-text-secondary);
    background: var(--ob-bg);
    border: 1px solid var(--ob-border);
    padding: 14px 24px;
    border-radius: 4px;
    margin-bottom: 32px;
  }

  .cta-code code {
    color: var(--ob-text);
  }

  .cta-actions {
    display: flex;
    justify-content: center;
    gap: 12px;
  }

  /* Responsive */
  @media (max-width: 900px) {
    .hero {
      padding: 120px 20px 80px;
    }

    .section {
      padding: 80px 20px;
    }

    .section-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .cta {
      padding: 80px 20px;
    }
  }

  @media (max-width: 640px) {
    .hero h1 {
      font-size: 28px;
    }

    .hero-sub {
      font-size: 15px;
    }

    .hero-actions {
      flex-direction: column;
      align-items: center;
    }

    .hero-actions .btn {
      width: 100%;
      max-width: 280px;
      justify-content: center;
    }

    .hero-badges {
      flex-direction: column;
      align-items: center;
    }

    .cta h2 {
      font-size: 24px;
    }

    .cta-code {
      font-size: 11px;
      padding: 12px 16px;
      word-break: break-all;
    }

    .cta-actions {
      flex-direction: column;
      align-items: center;
    }

    .cta-actions .btn {
      width: 100%;
      max-width: 280px;
      justify-content: center;
    }
  }
</style>
