---
interface Props {
  kind: string;
  name: string;
  description: string;
  tags: string[];
  status?: 'stable' | 'beta' | 'soon';
  href?: string;
}

const { kind, name, description, tags, status = 'stable', href } = Astro.props;

const Tag = href ? 'a' : 'div';
---

<Tag href={href} class={`component-card ${status === 'soon' ? 'soon' : ''}`} data-status={status}>
  <div class="component-header">
    <div class="component-kind">{kind}</div>
    {status !== 'stable' && (
      <span class={`status-badge badge-${status}`}>{status}</span>
    )}
  </div>
  <h3>{name}</h3>
  <p>{description}</p>
  <div class="component-tags">
    {tags.map((tag) => (
      <span class="component-tag">{tag}</span>
    ))}
  </div>
</Tag>

<style>
  .component-card {
    display: flex;
    flex-direction: column;
    background: var(--ob-bg-subtle);
    padding: 20px;
    transition: background 0.15s ease;
    height: 100%;
    min-height: 0;
  }

  a.component-card:hover {
    background: var(--ob-bg-muted);
  }

  .component-card.soon {
    opacity: 0.5;
  }

  .component-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
    flex-shrink: 0;
  }

  .component-kind {
    font-family: var(--ob-font-display);
    font-size: 10px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--ob-text-tertiary);
  }

  .status-badge {
    font-family: var(--ob-font-display);
    font-size: 9px;
    font-weight: 500;
    padding: 2px 6px;
    border-radius: 2px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .badge-beta {
    color: var(--ob-amber);
    background: rgba(251, 191, 36, 0.1);
  }

  .badge-soon {
    color: var(--ob-amber);
    background: rgba(251, 191, 36, 0.1);
  }

  h3 {
    font-family: var(--ob-font-display);
    font-size: 14px;
    font-weight: 500;
    margin-bottom: 6px;
    color: var(--ob-text);
    flex-shrink: 0;
    letter-spacing: -0.01em;
  }

  p {
    font-family: var(--ob-font-sans);
    font-size: 13px;
    color: var(--ob-text-secondary);
    margin-bottom: 14px;
    line-height: 1.5;
    flex: 1;
  }

  .component-tags {
    display: flex;
    gap: 4px;
    flex-wrap: wrap;
    margin-top: auto;
    flex-shrink: 0;
  }

  .component-tag {
    font-family: var(--ob-font-display);
    font-size: 10px;
    font-weight: 500;
    color: var(--ob-text-tertiary);
    padding: 2px 6px;
    background: var(--ob-bg-elevated);
    border-radius: 2px;
  }

  @media (max-width: 640px) {
    .component-card {
      padding: 16px;
    }
  }
</style>
