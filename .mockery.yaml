with-expecter: true
dir: "{{.InterfaceDir}}/mocks"
filename: "{{.InterfaceName | snakecase}}_mock.go"
mockname: "Mock{{.InterfaceName}}"
outpkg: mocks
packages:
  github.com/stackbound/stackbound/internal/schema:
    interfaces:
      Schema:
  github.com/stackbound/stackbound/internal/parser:
    interfaces:
      Node:
